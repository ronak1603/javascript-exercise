function maxNumber(val1, val2) {
    return (val1 > val2) ? val1 : val2 //ternary operator

    // if (val1 > val2)         
    //     return val1             // normal syntax
    // else {
    //     return val2
    // }
}

let maxNumber = maxNumber(300, 400)